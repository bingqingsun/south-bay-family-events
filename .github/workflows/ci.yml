name: Verify South Bay Family Finds
# This workflow is intentionally read-only: generated activity data is
# validated separately by the refresh workflow and does not need a second CI run.

on:
  pull_request:
    paths:
      - 'scripts/**'
      - 'data/sources.json'
      - 'analytics.js'
      - 'collection-runtime.js'
      - 'collections.js'
      - 'collections.css'
      - 'data/collections.js'
      - 'collections/**'
      - 'scripts/collection-runtime.test.mjs'
      - 'map-navigation.js'
      - 'scripts/map-navigation.test.mjs'
      - 'privacy.html'
      - '.github/workflows/**'
  push:
    branches: [main]
    paths:
      - 'scripts/**'
      - 'data/sources.json'
      - 'analytics.js'
      - 'collection-runtime.js'
      - 'collections.js'
      - 'collections.css'
      - 'data/collections.js'
      - 'collections/**'
      - 'scripts/collection-runtime.test.mjs'
      - 'map-navigation.js'
      - 'scripts/map-navigation.test.mjs'
      - 'privacy.html'
      - '.github/workflows/**'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check refresh-script syntax
        run: |
          node --check scripts/update-events.mjs
          node --check scripts/cupertino-detail-date.mjs
          node --check scripts/cupertino-audience.mjs
          node --check scripts/link-health.mjs
          node --check map-navigation.js
          node --check age-policy.js
          node --check collection-runtime.js
          node --check collections.js
          node --check data/collections.js
      - name: Run policy and data-contract tests
        run: |
          node scripts/test-event-summary-engine.mjs
          node scripts/cupertino-detail-date.test.mjs
          node scripts/cupertino-audience.test.mjs
          node scripts/age-policy.test.cjs
          node scripts/test-summary-output.mjs
          node scripts/classification.test.mjs
          node scripts/cost-policy.test.mjs
          node scripts/movie-policy.test.mjs
          node scripts/recommendation.test.mjs
          node scripts/map-navigation.test.mjs
          node scripts/collection-runtime.test.mjs
          node scripts/link-health.test.mjs
